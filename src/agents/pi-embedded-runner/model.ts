// Phase 0 stub — model resolution.

export function resolveModel(
  _provider: string,
  _model: string,
  _override?: unknown,
  _cfg?: unknown,
): { model: string | undefined; error?: string } {
  return { model: undefined, error: "model resolution not implemented in Phase 0" };
}
